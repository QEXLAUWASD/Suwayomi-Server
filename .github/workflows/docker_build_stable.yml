name: Docker Build Stable

on:
  workflow_dispatch:
  # Disabled: release trigger removed (no Docker repo configured)
  # release:
  #   types: [published]
  
jobs:
  build_publish_docker_container:
    if: false  # Disabled: no Docker repo configured
    runs-on: ubuntu-latest

    steps:         
      - name: run docker build and publish script
        run: |
          curl -X POST -H "Accept: application/vnd.github.v3+json" -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" -d '{"ref":"main", "inputs":{"tachidesk_release_type": "stable"}}' https://api.github.com/repos/QEXLAUWASD/docker-tachidesk/actions/workflows/build_container_images.yml/dispatches
          
